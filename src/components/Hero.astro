<section class="pt-28 pb-16 bg-gradient-to-br from-white via-blue-50/40 to-purple-50/30 relative">
  <!-- Subtle dot pattern overlay -->
  <div class="absolute inset-0 opacity-[0.05] bg-[radial-gradient(#3b82f6_1px,transparent_1px)] [background-size:20px_20px] pointer-events-none"></div>
  
  <!-- Subtle gradient blob -->
  <div class="absolute -top-24 -right-24 w-96 h-96 bg-gradient-to-br from-primary-300/20 via-purple-300/20 to-transparent rounded-full blur-3xl pointer-events-none"></div>
  <div class="absolute -bottom-24 -left-24 w-96 h-96 bg-gradient-to-tr from-primary-300/20 via-blue-300/20 to-transparent rounded-full blur-3xl pointer-events-none"></div>
  
  <div class="max-w-5xl mx-auto px-8 sm:px-12 lg:px-16 relative z-10">
    <div class="text-center">
      <div class="inline-flex items-center px-3 py-1.5 rounded-full bg-primary-100/60 backdrop-blur-md border border-primary-200/60 text-primary-700 text-xs font-medium mb-6 relative overflow-hidden">
        <div class="absolute inset-0 bg-gradient-to-r from-transparent via-primary-200/20 to-transparent animate-pulse"></div>
        <svg class="relative w-3 h-3 mr-2 text-primary-500" fill="currentColor" viewBox="0 0 24 24">
          <path d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path>
        </svg>
        <span class="relative">New! WhatsApp Integration</span>
      </div>
      
      <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-6 animate-slide-up leading-tight">
        Transform your data into<br>
        <span class="bg-gradient-to-r from-primary-500 via-primary-600 to-purple-600 bg-clip-text text-transparent">
          insightful conversations
        </span>
      </h1>
      
      <p class="text-lg text-gray-600 mb-8 max-w-2xl mx-auto animate-slide-up leading-relaxed">
        Create intelligent AI chatbots that understand your business data and provide instant, accurate responses to your customers 24/7.
      </p>
      
      <div class="max-w-xl mx-auto mb-8 animate-slide-up">
        <div class="relative group rounded-xl overflow-hidden">
          <!-- Gradient border effect with inset-0 to fix corner glitch -->
          <div class="absolute inset-0 bg-gradient-to-r from-primary-400 to-purple-500 rounded-xl opacity-40 group-hover:opacity-60 transition duration-500"></div>
          
          <!-- Glossy highlight effect -->
          <div class="absolute -inset-full h-full w-1/2 z-10 block transform -skew-x-12 bg-gradient-to-r from-transparent to-white opacity-20 group-hover:animate-shine"></div>
          
          <div class="relative bg-white/90 backdrop-blur-md rounded-xl border-2 border-primary-200/70 p-2 z-20">
            <div class="flex flex-col sm:flex-row gap-2">
              <input 
                type="url" 
                placeholder="Enter your knowledge base URL..."
                class="flex-1 px-4 py-3 border-0 rounded-lg focus:ring-2 focus:ring-primary-500/30 outline-none text-sm bg-gray-50/50 backdrop-blur-sm"
              >
              <button class="bg-gradient-to-r from-primary-500 to-primary-600 hover:from-primary-600 hover:to-primary-700 text-white px-6 py-3 rounded-lg font-semibold text-sm transition-all duration-200 border-2 border-primary-400/70">
                Create chatbot
                <svg class="inline-block w-3 h-3 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                </svg>
              </button>
            </div>
          </div>
        </div>
      </div>
      
      <p class="text-gray-500 text-sm">
        No credit card required • Setup in 5 minutes • 14-day free trial
      </p>
    </div>
  </div>
</section>